apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trustable-ing
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: $TRAEFIK_ENTRYPOINTS
$T    cert-manager.io/cluster-issuer: letsencrypt-prod
$T    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
    - host: $TRUSTABLE_HOST
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trustable-svc
                port:
                  number: 8910
$T  tls:
$T    - hosts:
$T        - $TRUSTABLE_HOST
$T      secretName: trustable-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencode-ing
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: $TRAEFIK_ENTRYPOINTS
$T    cert-manager.io/cluster-issuer: letsencrypt-prod
$T    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
    - host: $OPENCODE_HOST
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trustable-svc
                port:
                  number: 4096
$T  tls:
$T    - hosts:
$T        - $OPENCODE_HOST
$T      secretName: opencode-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vite-ing
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: $TRAEFIK_ENTRYPOINTS
$T    cert-manager.io/cluster-issuer: letsencrypt-prod
$T    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
    - host: $VITE_HOST
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trustable-svc
                port:
                  number: 5173
$T  tls:
$T    - hosts:
$T        - $VITE_HOST
$T      secretName: vite-tls
